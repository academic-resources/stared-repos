"""Detect the most likely words from a text without spaces and a given list of words.
Example
Text: filesaveas
Words: [files, file, save, as]
Solution: file save as

Text: tomorrowneverdies
Words: [tom, tomorrow, row, ever, never, die, dies]
Solution: tomorrow never dies

Assume each text has only one unique split.
"""


class Solution:
    def infer_spaces(self, text, words):
        pass
