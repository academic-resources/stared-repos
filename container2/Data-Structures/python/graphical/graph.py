class Node:
    def __init__(self, data):
        self.data = data
        self.edges = [];

# Graphical Data Structure, composed of nodes(vertices), connections(edges) and costs to traverse those connections
class Graph:
    def __init__(self):
        self.vertexes = [];

    # Adds a vertex to the list
    def addVertex(self):
        return

    # removes a vertex from the list, must also remove all connections to it
    def removeVertex(self):
        return

    # Adds a connection between two nodes, with associated cost
    def addEdge(self, startVertex, endVertex):
        return

    # Removes a connection between two nodes
    def removeEdge(self, vertex):
        return
