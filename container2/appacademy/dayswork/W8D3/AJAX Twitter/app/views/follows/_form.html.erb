 <% init_follow_state = current_user.follows?(user) ? 'followed' : 'unfollowed' %>

<button data-user-id=<%= user.id %> data-initial-follow-state=<%= init_follow_state %> class="follow-toggle"></button>

