/*
`Intl` object is the namespace for the ECMAScript Internationalization API,
which provides language sensitive string comparison, number formatting,
and date and time formatting.

The `Intl` object provides access to several constructors as well as
functionality common to the internationalization constructors and
other language sensitive functions.
 */

/*
PROPERTIES

Intl.<property>

+ Collator
constructor for collators, which are objects that enable
language-sensitive string comparision

+ DateTimeFormat

+ ListFormat

+ Locale
objects that represents a Unicode locale identifier

+ NumberFormat

+ PluralRules
objects that enable plural-sensitive formatting
and language-specific rules for plurals

+ RelativeTimeFormat
 */
